# GoogleAPI French Audio Generator

This small tool automates generating french audio translations for large word lists.

## API

The API allows calling this generation tool from another module.

## CLI

The CLI allows generating these files from the command line.
